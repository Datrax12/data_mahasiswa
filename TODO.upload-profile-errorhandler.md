# TODO: Tambahkan global error handler untuk upload foto profile

- [ ] Tambahkan `@app.errorhandler(Exception)` dan `@app.errorhandler(500)` di `app.py` untuk memastikan traceback muncul di logs Vercel.
- [ ] Redeploy.
- [ ] Coba upload ulang, lalu lihat logs Vercel.

